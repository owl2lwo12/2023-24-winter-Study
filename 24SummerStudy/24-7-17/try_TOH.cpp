#include <string>
#include <vector>

using namespace std;

vector<int> Tower_of_hanoi(int n, int start, int end) {
    if (n == 1)
}

vector<vector<int>> solution(int n) {
    vector<vector<int>> answer;
    return answer;
}